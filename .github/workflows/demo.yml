name: Copy my_file.json to tests subdirectory
on: [push, pull_request, workflow_dispatch]
jobs:
  run:
    runs-on: [ubuntu-latest]
    steps:
    - name: Checkout 🛎️
      uses: actions/checkout@v2

    - name: Make copy of samples.txt 📚
      run: |
        chmod u+x "${GITHUB_WORKSPACE}/files/samples.txt"
        bash "${GITHUB_WORKSPACE}/files/samples.txt"

